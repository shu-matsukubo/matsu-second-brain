interface:
  display_name: "Knowledge Viewerを同期"
  short_description: "stateとtree SHAを確認し既存Viewerだけを同期"
  default_prompt: "Use $sync-knowledge-viewer to update the existing Knowledge Viewer only when committed knowledge or indexes are not yet synchronized."
