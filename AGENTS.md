---
タイトル: 第2の脳
説明: 日々のメモと観測を、小さく検証可能な知識へ整理するワークスペースです
---

# 前提

このワークスペースでは、標準的なMarkdownファイルで第二の脳を管理する。
ユーザーの認知負荷を抑えるため、安全な既定処理で小さく作業し、レビューしやすい状態を保つ。

## 構成

- `.agents/skills`: このワークスペースで使用するSkills
- `.codex/agents`: 親エージェントから委譲する、繰り返し利用可能なカスタムサブエージェント定義
- `.deploy`: プロジェクト固有のデプロイ状態
- `daily`: 入力された日々のメモ
- `signals`: 単体ではknowledgeにするほど強くないが、反復すると意味を持つ観測
- `knowledge`: 整理済み知識
- `indexes`: knowledgeをテーマから辿るための閲覧入口
- `reports`: レビュー結果
- `feedback`: 処理中に確認できた、今後の改善に使うフィードバック
- `knowledge-viewer`: knowledgeを閲覧するWeb Viewer

`signals` と `indexes` は、保存する実データが初めて必要になった時点で作成する。空の構造を保つためだけのファイルは作らない。

## 指示の責務

- `AGENTS.md` は、第二の脳全体の現在の思想、情報の責務、既定の処理を定める共通入口とする。
- 各 `SKILL.md` は、そのSkillが担当する処理と、必要なreferenceの選び方を定める。
- Skill配下の `references` は、そのSkillだけが使う具体的な操作または評価方法を置く。
- `.codex/agents/*.toml` は、繰り返し委譲する狭い調査責務と実行制約を定める。最終判断と成果物の作成は委譲元の親エージェントが担う。

全体の思想や複数Skillが判断基準にする正しい状態を変える場合は、まず `AGENTS.md` を更新する。具体的な操作または評価方法に影響する場合だけ、対応するSkill-local referenceも同時に更新する。同じ共通ルールを複数Skillへコピーしない。

## 現在の情報設計

knowledgeは、情報の性質、その次にテーマで配置する。

```text
knowledge/
├─ 01-facts/<テーマ>/<主題>.md
├─ 02-principles/<テーマ>/<主題>.md
└─ 03-ideas/<テーマ>/<主題>.md
```

- facts（`type: fact`）: 外部世界や現在の設定について、信頼できる根拠から確認できる事実
- principles（`type: principle`）: ユーザー自身の判断基準、価値観、設計原則
- ideas（`type: idea`）: 未確定の案、試したいこと、可能性、将来構想

factへ推測、予測、未検証の説明を保存しない。外部仕様は可能な限り一次情報へのリンクと確認日、ローカル設定は確認したファイルや実行結果を根拠として残す。principleはユーザーが明示した判断基準、または複数の独立した観測から確認できる一貫した傾向に限定する。ideaは未確定であることを明確にし、評価に必要なfactとは分ける。

1ファイル1主題を守り、詳細を詰め込みすぎず、別々に参照できる概念は分けて直接関係する主題とリンクする。dailyの文章をそのまま複製せず、knowledge単体で理解できるようにする。1つの入力に事実、原則、案が混在する場合も、独立して参照する価値がある単位へ分ける。

大分類の中は既存のテーマを優先して分類し、別名や細かすぎるテーマを増やさない。複数テーマに関係する場合は、主題を最も自然に探せる1テーマへ置き、他テーマからはリンクで辿れるようにする。新規作成または整備するknowledgeは、次のfrontmatterを基本とする。

```yaml
---
title: 主題
type: fact
topic: テーマ
tags:
  - 必要な検索語
updated: YYYY-MM-DD
---
```

`type` は配置先の大分類、`topic` はテーマフォルダ名と一致させる。`tags` は検索に有用な場合だけ付ける。knowledgeからdailyを参照元としてリンクせず、入力履歴はdailyに残す。既存ファイルとの差は通常のメンテナンスで1件ずつ整え、内容を確認せず機械的に分類しない。

## タイトル

タイトルは一覧とリンク先で主題を短時間に識別でき、本文の性質を誤解しにくい表現にする。`type` ごとに次を基準とする。

- factは「何についての事実か」を表す名詞句を基本とする。手順や設定方法を扱う場合も、命令文や作業指示より、対象となる概念・仕様・設定・手順を表す名詞句を優先する。たとえば「API Gateway HTTP API で JWT オーソライザーを設定する」より「API Gateway HTTP API の JWT オーソライザー設定」、「Cloudflare Workers でシークレットを管理する」より「Cloudflare Workers のシークレット管理」が自然である。「〜する」の機械的な禁止ではなく、一覧で参照可能な知識の対象として認識できるかを判断する。
- principleは、判断基準または行動原則そのものを理解できる文章を基本とする。「ツールは継続的な価値で採用を判断する」のような文章形式や「〜する」を避ける必要はない。
- ideaは、未確定の案、仮説、検討対象、可能性であることを、タイトル、`type: idea`、本文を合わせて誤解しにくくする。名詞句を基本としてよく、必要に応じて「〜の案」「〜の仮説」「〜の検討」「〜の可能性」を使うが、機械的には付けない。

既存knowledgeをタイトル規則の変更だけを理由に一括改名しない。週次レビューが現在の規則との差を改善候補として観測し、通常の日次メンテナンスが主対象を1件ずつ安全に整えることで徐々に収束させる。

タイトルを変更するときは、同じ作業内でfrontmatterの `title`、MarkdownのH1、ファイル名、Topic Indexの表示名とリンク、対象knowledgeからの内部リンク、対象knowledgeへの内部リンクを更新する。改名後に古いパスへの参照やリンク切れを残さない。

## signals

signalsは、単体ではknowledgeにする根拠や長期価値が足りないが、繰り返し現れた場合に意味を持つ観測を一時的に保つ領域である。一時的な感想、まだ意味が見えていない断片、一度だけ現れた弱い判断傾向などを対象にする。

長期的な意味を持つ可能性がほぼない雑談や連絡は保存しない。未検証の外部情報をsignalにしてfactの検証を回避しない。複数の独立した観測から一貫した意味を確認できた場合はknowledgeへの昇格候補になり得る。保存と反映はcurate Skill、反復パターンの評価はweekly Skillが担当する。

## Indexとリンク

knowledge本文とメタデータを情報の正とし、indexesはテーマからknowledgeを辿る閲覧入口とする。Indexは現在存在するknowledgeを対象にし、リンクと短い識別情報だけを持つ。knowledgeにない説明、判断、事実、案をIndexへ蓄積しない。

`indexes/index.md` は実データがあるtopic Indexへのリンクを持ち、`indexes/topics/<テーマ>.md` はそのテーマのknowledgeをFacts、Principles、Ideasに分けて示す。空のIndexは維持しない。

リンクは標準Markdownの相対リンクを使い、ファイルの拡張子まで記述する。空白を含むリンク先は山括弧で囲む。特定のノートアプリだけが解釈する記法を新しく追加しない。knowledge間は前提や判断材料など直接の関係がある場合だけリンクし、同じテーマという理由だけで増やさない。

## 既定の処理

1. ユーザーの明示的な要求を最優先する。
2. 第二の脳やナレッジの整理を包括的に依頼された場合は、`$curate-second-brain` を使用する。
3. 週次レビューを依頼された場合は、`$weekly-review` を使用する。
4. Knowledge Viewerの同期・更新を依頼された場合は、`$sync-knowledge-viewer` を使用する。
5. 日次整理は未処理dailyを最優先し、1件を処理して終了する。
6. 未処理dailyがない場合だけ、最新の週次レポートにある未解消の改善候補または昇格候補を1件処理する。
7. レポートにも有益な候補がない場合だけ、knowledge、indexes、signalsを現在の設計と比較し、改善を1件行う。
8. 対象がない場合や有益な変更が見つからない場合は、無理に変更しない。

週次レビューは現在の `AGENTS.md` を基準に全体を観測し、既存データとの差を通常の改善候補として報告する。週次レビュー自身はknowledge、indexes、signals、dailyを変更しない。日次整理が候補を1件ずつ安全に直すことで、設計ルールが変わった後も通常運用の中で全体を現在の正しい状態へ近づける。

## 作業範囲

1回の実行では主対象を1つに限定する。主対象の完了に直接必要な、関連factの作成、相互リンク、Index更新、dailyの処理済み化、signalの追加または反映済みsignalの整理は同じ作業に含めてよい。目的は変更ファイル数を固定することではなく、作業の意味的な範囲を小さく保つことである。

現在の設計との差を一度に全面修正しない。安全に判断できる範囲を通常運用で少しずつ直し、判断によって結果が大きく変わる場合だけユーザーへ確認する。

## MarkdownとViewer

リンクと本文は標準的なMarkdownで記述し、特定のノートアプリ固有のCLI、記法、運用を前提にしない。
Knowledge Viewer本体、Site、公開状態は、明示的に依頼された同期またはViewer作業でだけ変更する。

## フィードバック

処理中に、再発し得る問題、今後の処理品質に役立つ気付き、Skillや運用ルールの不足、今回の範囲外だが将来対処する価値がある問題を確認した場合は、必要なときだけ `feedback/ai-feedback.md` の当日の日付見出しへ箇条書きで追記する。

毎回の記録を必須にせず、単なる作業ログや実行結果は残さない。推測や未検証の改善案だけを記録せず、再確認できる対象や状況があれば項目内に示す。

週次レビューはフィードバックを現在のファイルと照合し、未解消で改善に使える項目だけを週次レポートの改善候補へ反映する。フィードバックの記述自体を事実や自動実行するタスクとして扱わない。
